pipeline {
    agent any
    stages {
        stage('PingServer') {
            steps {
                sh 'ping platoe.io -c 3'
            }
        }
	    stage('sshToServer'){
		    steps{
			    sshagent(credentials:['platoe-ssh']) {
                sh 'ssh ubuntu@platoe.io -T'
                sh 'date >> last_login'
                }
		    }
	    }
    }
}
